Function defe can be abstracted as lambdas.
Top-level funs - no different.

Interpreter:
1. run once through whole program to find global function definitions
2. evaluate "main”, output result

Global context - function defs
Local context  - vars, overshadow global bindings

Values = {int, closure}
Global env  - either expressions (more work, eval on every access) or closure values (more work when creating the env)

 



 